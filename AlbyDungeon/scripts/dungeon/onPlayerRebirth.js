/**
 * DUNGEON type script for DUNGEON AlbyDungeon
 * 
 * onPlayerRebirth is a script containing instructions
 * on how to deal with dead players trying to rejoin
 * the dungeon session they weer a part of.
 *
 * @author      CONSOLE
 * @version     0.1
 * @type        DUNGEON
 * @dungeon     AlbyDungeon
 *
 * @param {Player}          player     SCRIPT BINDING
 * @param {ScriptManager}   sm         SCRIPT BINDING
 * @param {LoadedDungeon}   dungeon    SCRIPT BINDING
 */
function main() {
    // print("onPlayerRebirth called");
    player.sendMessage(`unimplemented`);
}
main();